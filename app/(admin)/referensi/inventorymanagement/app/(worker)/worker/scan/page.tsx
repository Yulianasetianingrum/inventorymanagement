import { Card } from "@/components/ui/card";

export default function ScanPage() {
  return (
    <Card>
      <h1 style={{ fontSize: "1.4rem", fontWeight: 700, marginBottom: "0.5rem" }}>Scan</h1>
      <p style={{ color: "#6b7280" }}>Placeholder scanner (barcode/QR).</p>
    </Card>
  );
}
